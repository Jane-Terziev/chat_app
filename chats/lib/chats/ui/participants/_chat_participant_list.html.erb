<div id="chatParticipantList">
  <% chat.chat_participants.each do |participant| %>
    <%= render 'chats/ui/participants/chat_participant_item', chat_id: chat.id, participant: participant %>
  <% end %>
</div>